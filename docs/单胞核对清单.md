# 批量构型单胞核对清单

进度总表 + **各案成功策略**：[`批量构型STEP生成情况明细.md`](./批量构型STEP生成情况明细.md) §2  
（**1x1 以本机勾选为准**；**444 / 策略以 `*_qc.json` 为准**。上次对齐：`2026-07-28`；含 `af2q1_deq1p5_k1` 作废）

根目录：`output/cad/批量构型/`

在 Cursor / VS Code 中打开本文件，点击 `- [ ]` 即可打勾（变成 `- [x]`）。

## 勾选含义

- `[x]`：**单胞合格**（无斜面/尖楔问题）
- `[ ]`（已打开过）：**strut1 合格，单胞有斜面** → 需重做 1x1
- 未拉回 / 未打开：见「本机已删」节

## 通用检查项

- [ ] 打开 `{id}_1x1.step`
- [ ] 八分之一 / paper-box 外形完整，无碎块游离
- [ ] 杆端无尖楔（外端面应近似平切，对齐 strut1）
- [ ] 对照同目录 `{id}_strut1.step`：端部形状一致
- [ ] 无异常薄片 / 毛刺贴在杆尖

## 逐案（本机已查）

### 合格 `[x]`（10；服上 444 曾 QC=ok 单实体，2026-07-19；细杆案已剔除）

- [x] `af1q1_deq2_k1` — 1x1 OK；**444 OK**
- [x] `af2q0_deq2_k1p5` — 1x1 OK；**444 OK**
- [x] `af2q0p5_deq2_k1p5` — 1x1 OK；**444 OK**
- [x] `af2q1p5_deq2_k1p5` — 1x1 OK；**444 OK**
- [x] `af3q1_deq2_k1` — 1x1 OK；**444 OK**
- [x] `af2q1_deq2_k1` — `ocp_centre_stub_corner_ext`；**444 OK**（`noclip_batch64`）
- [x] `af2q1_deq2_k1p5` — 同上；**444 OK**
- [x] `af2q1_deq2_k2` — 同上；**444 OK**
- [x] `af2q0p5_deq2_k2` — `both_end+ext=3`；**444 OK**（`noclip_batch64`）
- [x] `af2q1_deq2p5_k1` — face-mate `ext=2.5`；**444 OK**（`noclip_batch64`）

### 作废 / 重生待查

- [ ] `af2q1_deq1p5_k1` — **2026-07-29**：空缺定位为顺序 fuse **最后一步 idx6**；改为序 `0..7`（先 6 后 7）后 **B 目视 OK**。已写入 `OCTANT_SEQUENTIAL_FUSE_ORDER`，主 `_1x1.step` 已换成 B。请再确认一遍后勾合格并生成 444。**禁加球**。

## 本机已删 — 曾有仿真 / 当时在跑（未查）

服务器仍有旧 CAD（含可能的旧 444）；**本次未清**；未在本机目视。

- [ ] `af2q0_deq2_k1` — 仿真已完成
- [ ] `af2q0p5_deq2_k1` — 仿真已完成
- [ ] `af2q1p5_deq2_k1` — 仿真已完成
- [ ] `af2q0_deq2_k2` — 仿真已完成
- [ ] `af2q1p5_deq2_k2` — 当时求解中

## 备注

- 2026-07-17 23:11：曾清 11 合格案服上 `*_444.step`（防旧阵列混淆）
- 2026-07-19：11 案按批处理梯子（含 face-mate + `noclip_batch64`）**全部重生并通过 QC**；细节见 [`批量构型STEP生成说明.md`](./批量构型STEP生成说明.md) §3
- 2026-07-24：16/16 CAD `status=ok`；成功路径归类（11 noclip / 2 gmsh / 2 旧 OCP / 1 复用）见明细 §1–§2
- 2026-07-28：`af2q1_deq1p5_k1` 单胞复检不合格 → **作废**（删 STEP；仿真产物标 void，不入主对比）
- 2026-07-29：细杆空缺定位为 fuse 序把 idx6 放最后；改为 `0..7` 后目视 OK，已改代码默认序并重生 `_1x1`
- 发现新问题：在案名后补一句，例如 `← 杆端尖楔`
